#include <stdio.h>
#include <stdlib.h>

int main()
{
    int total, soma, media, escopo, mais1;
    char ;
    printf("Digite um número: ");
    scanf("%d", &escopo);
    soma = 0;
    soma = soma + escopo;
    total = 0;
    total += 1;
    // return 0;
}
